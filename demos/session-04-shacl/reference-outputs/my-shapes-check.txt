python check_my_shapes.py, pySHACL 0.40.1, 2026-09-25 (under 2 s per run).

Blank my_shapes.ttl:
  Y1  not yet: not written yet (the shape has a target but no constraint).
  Y2  not yet: not written yet (the shape has a target but no constraint).
  Y3  not yet: not written yet (the shape has a target but no constraint).
  
  0 of 3 right.

Solutions (solutions/my_shapes_solutions.ttl):
  Y1  right: flags exactly 1447291369.7, 1447385217.7.
  Y2  right: flags exactly 1447311670.7.
  Y3  right: flags exactly 1447135386.7.
  
  3 of 3 right.

Common wrong answers, each run through the checker:
  Y1 with sh:minCount only
    Y1  not yet: You catch the order with no customer, not the one with two. Add sh:maxCount 1.
  Y1 with sh:maxCount only
    Y1  not yet: You catch the order with two customers, not the one with none. Add sh:minCount 1.
  Y2 with plain string bounds
    Y2  not yet: Too many orders flagged. If your bounds are plain text like "2013-01-01", SHACL cannot compare a date with text, so every date fails: write "2013-01-01"^^xsd:date. Otherwise check that the bounds are inclusive and cover the whole of 2013.
  Y3 comparing STR(?c) with "V44_3"
    Y3  not yet: You also flag 1447296446.7, a CRF order that IS carried by V44_3. Compare ?c with the carrier's IRI, <https://ul.edu.lb/kr/id/carrier/brunel/V44_3>, not with the text "V44_3".
  Y3 with a typo in the target (ul:Orders)
    Y3  not yet: your shape targets no order, so it checks nothing. Keep sh:targetClass ul:Order.

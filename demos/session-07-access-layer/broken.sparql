# A query a model really could write: ul:shippedBy sounds right, but the
# Brunel graph calls it ul:carriedBy.
SELECT (COUNT(?order) AS ?n) WHERE {
  ?order ul:shippedBy <https://ul.edu.lb/kr/id/carrier/brunel/V44_3> .
}

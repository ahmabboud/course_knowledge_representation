# Shared environment for Sessions 1 through 4.
#
# Requires Python 3.12 or 3.13. Session 5's Morph-KGC stack and Session 6's
# machine-learning stack each have their own requirements file because their
# dependency constraints conflict with this shared environment.

# ydata-profiling 4.18.4 still imports pkg_resources, supplied by setuptools.
setuptools==80.9.0

# Data download and profiling — Session 1
pandas==2.3.3
ydata-profiling==4.18.4
openpyxl==3.1.5
matplotlib==3.10.0

# RDF, SPARQL, and containers — Sessions 2 onward
requests==2.34.2
rdflib==7.6.0
SPARQLWrapper==2.0.0
pyoxigraph==0.5.11
neo4j==6.3.1
pyshacl==0.40.1

# Notebooks and shared utilities
jupyterlab==4.6.4
ipykernel==7.3.0
jupytext==1.19.5
networkx==3.7
pytest==9.1.1
python-dotenv==1.2.3
